<%= form_for(@micripost) do |f| %>
  <% if @micripost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micripost.errors.count, "error") %> prohibited this micripost from being saved:</h2>

      <ul>
      <% @micripost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contend %><br />
    <%= f.text_field :contend %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
